-r requirements.txt

# ------------------------------------------------------------------
# Test suite
# ------------------------------------------------------------------
pytest>=8.0
anyio>=4.4      # powers the @pytest.mark.anyio tests already in the suite
httpx>=0.27     # required by fastapi.testclient.TestClient