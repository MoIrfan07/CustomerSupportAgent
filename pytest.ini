[pytest]
pythonpath = app app/adapters .
markers =
    integration: exercises a live external service (real Qwen/DashScope calls) — excluded by default, run explicitly with `-m integration`
addopts = -m "not integration"
